# Core dependencies
numpy>=1.23.0
pandas>=1.4.0
scikit-learn>=1.3.0
treelib>=1.7.0

# Optional ML dependencies (for prediction models)
torch>=1.9.0; extra == 'ml'
matplotlib>=3.7.0; extra == 'ml'

# Platform logging (assumed to be available)
# rcabench-platform>=2.0.0
